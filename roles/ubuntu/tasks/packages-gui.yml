---
- name: Install GUI packages
  become: true
  apt:
    name: '{{ packages }}'
    state: present
    force_apt_get: yes
  vars:
    packages:
      - emacs
      - evolution
      - galculator
      - git-gui
      - gnome-session
      - nextcloud-desktop
      - remmina
      - vim-gtk3
