---
- name: Install GUI packages
  become: true
  dnf:
    name: '{{ packages }}'
    state: present
  vars:
    packages:
      - emacs
      - galculator
      - git-gui
      - liberation-fonts
      - remmina
      - rxvt-unicode
      - vim-X11
      - xterm
